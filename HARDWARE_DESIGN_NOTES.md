# RP2350A USB ISP Dongle - 하드웨어 설계 및 회로도 리뷰 요약

본 문서는 **RP2350A 기반 USB ISP 동글 보드**(`RP2350A_ISP.kicad_sch`)의 회로 설계 검토 내역 및 최종 확정 사양을 정리한 문서입니다.  
맥미니(macOS) 또는 다른 개발 환경에서 프로젝트를 열었을 때 바로 참조할 수 있도록 작성되었습니다.

---

## 1. 프로젝트 개요
* **메인 MCU**: Raspberry Pi RP2350A (QFN-60 패키지)
* **용도**: PC USB와 연결되어 외부 스케일러(Scaler) 보드를 I2C ISP 방식으로 펌웨어 라이팅/제어하는 USB ISP 동글
* **EDA 도구**: KiCad 8 / 9
* **주요 인터페이스**:
  * PC 연결: USB 2.0 Full Speed
  * 타깃 시스템 연결: ISP 포트 (I2C: SCL, SDA, GND)
  * 디버깅 인터페이스: 6핀 올인원 포트 (SWD + 하드웨어 리셋 + UART 시리얼 콘솔)

---

## 2. 주요 회로별 확정 사양 및 수정 내역

### ① RP2350A 내장 스위칭 레귤레이터 (1.1V 코어 전원)
RP2350은 구형(RP2040)의 LDO와 달리 **강압형 스위칭 레귤레이터(Buck SMPS)**를 내장하고 있으므로 인덕터가 필수입니다.
* **Pin 48 (`VREG_LX`)**: 스위칭 출력단 ➔ **3.3 µH 파워 인덕터(L1)** 직렬 연결
* **Pin 50 (`VREG_FB`)**: 전압 피드백 ➔ 인덕터를 통과한 **1.1V 라인에 연결**
* **1.1V 출력단 커패시터**: **10 µF 세라믹 커패시터** 1개 + 0.1 µF 디커플링 커패시터 3개 (C4, C5, C6)
* **권장 인덕터 부품 (DigiKey 구매 가능)**:
  * **Murata `LQM21PN3R3MGRD`** (3.3 µH, 0805, 정격 1.0A, DCR 188mΩ)
  * **TDK `MLZ2012N3R3LT000`** (3.3 µH, 0805, 정격 750mA, DCR 150mΩ)

### ② USB 2.0 Full-Speed 인터페이스 (D+ / D-)
* **커넥터**: CN1 (USB Type-B / Micro-B)
* **ESD 보호 소자**: **`AZ1045-04F`** (초저용량 관통형 TVS 다이오드 어레이, 기생 용량 ~0.55 pF)
  * *초기 BZX84C5V6(제너 다이오드, 300pF)에서 발생하던 신호 왜곡 및 인식 불량 문제 완전 해결*
* **직렬 댐핑 저항**: **`R3, R4 = 27 Ω (±1%)`** (RP2350 내부 드라이버 임피던스 ~18Ω과 합쳐져 USB 규격 45Ω/90Ω 정합)
* **D+ 풀업 저항**: RP2350 칩 내부에 1.5 kΩ 소프트웨어 제어 풀업이 내장되어 있으므로 **외장 풀업 저항 미실장(정상)**

### ③ 메인 전원단 (USB 5V ➔ 3.3V)
* **LDO IC**: **`LM1117-3.3`** (SOT-223, 3.3V 고정 출력형)
* **입력단 커패시터**: 10 µF (USB 입력단 C1)
* **출력단 커패시터**: 10 µF (0805 또는 3216 세라믹 커패시터 필수 추가)

### ④ ISP 포트 (CN2: 20017WS-04, 스케일러 연결용)
* **Pin 1**: NC
* **Pin 2**: `ISP_SCL` (RP2350 GPIO0) ➔ 22 Ω 직렬 저항 + **4.7 kΩ 풀업 저항(+3.3V)**
* **Pin 3**: `ISP_SDA` (RP2350 GPIO1) ➔ 22 Ω 직렬 저항 + **4.7 kΩ 풀업 저항(+3.3V)**
* **Pin 4**: **`GND` (필수 연결)**
  * *외부 스케일러 보드와 공통 접지 기준선 형성*

### ⑤ 시리얼 플래시 및 BOOTSEL 회로
* **플래시 IC**: U3 (EN25Q80C / W25Q128 계열, QSPI 인터페이스)
* **결선 방식**: 라즈베리파이 공식 RP2350 레퍼런스 디자인 적용
  * RP2350 `QSPI_SS` ➔ 플래시 Pin 1 (`CS`) 직결
  * `QSPI_SS` 라인에서 **`1 kΩ (R6)` 직렬 저항 ➔ `SW2 (택트 스위치)` ➔ `GND`**
  * 부팅 시 Bootrom이 `QSPI_SS` 핀의 내부 풀업(~50kΩ) 상태를 읽으므로, 버튼 클릭 시 약 0.06V(LOW)로 안정적 인식.

### ⑥ 올인원 디버깅 포트 (CN3: 12505WR-06A00, 6핀)
GND 중복 핀을 정리하여 **SWD + 하드웨어 리셋 + UART 시리얼 콘솔**을 케이블 하나로 통합:
| Pin | 신호명 | RP2350 연결 핀 | 기능 설명 |
| :---: | :---: | :---: | :--- |
| **1** | **SWCLK** | Pin 24 (SWCLK) | SWD 클럭 (프로그래밍 및 디버깅) |
| **2** | **SWDIO** | Pin 25 (SWDIO) | SWD 데이터 라인 |
| **3** | **GND** | GND | 공통 접지 |
| **4** | **RUN** | Pin 26 (RUN / SW1) | 하드웨어 강제 리셋 (Connect Under Reset 복구용) |
| **5** | **UART1_TX**| Pin 12 (GPIO8) | MCU ➔ PC 시리얼 텍스트 로그 출력 (`printf`) |
| **6** | **UART1_RX**| Pin 13 (GPIO9) | PC ➔ MCU 명령어/CLI 입력 수신 |

### ⑦ 상태 표시 LED (D5, D6, D7, D8)
* **패키지**: 2012 (0805 Metric) SMD LED
* **구동 방식**: RP2350 `GPIO2, 3, 4, 5`에서 470 Ω 저항을 통해 직접 드라이빙 (Active HIGH)
* **전류**: 약 1.1 mA ~ 2.7 mA (RP2350 핀 허용 전류 12mA 이내로 매우 안전하고 밝기 적정)
* **풋프린트 극성**:
  * **1번 핀 (Cathode / 음극 / -)**: 실크스크린 ㄷ자 테두리 쪽 ➔ **GND** 연결
  * **2번 핀 (Anode / 양극 / +)**: ➔ **470 Ω 저항** 연결

---

## 3. PCB 아트웍(라우팅) 핵심 가이드
1. **스위칭 전원 루프 최소화**:
   * 인덕터 `L1(3.3µH)`과 10µF 커패시터는 RP2350의 Pin 48(`VREG_LX`) 및 Pin 50(`VREG_FB`) 바로 옆에 최단 배선으로 배치.
2. **USB 90Ω 차동 임피던스**:
   * CN1 ➔ TVS(`AZ1045`) ➔ 27Ω 저항 ➔ RP2350 핀 순서로 최단 거리 배치 및 D+/D- 쌍 배선(Differential Pair) 유지.
3. **RP2350 QFN-60 중앙 바닥 패드(Pin 61 Exposed Pad)**:
   * GND 비아(Via)를 최소 4~9개 촘촘히 뚫어 내층/바닥면 GND 플레인과 저임피던스 열/접지 연결 확보.
